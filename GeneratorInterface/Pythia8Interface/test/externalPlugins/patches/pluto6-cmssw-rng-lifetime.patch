diff --git a/include/PChannel.h b/include/PChannel.h
index 174dfd9..5a97fee 100644
--- a/include/PChannel.h
+++ b/include/PChannel.h
@@ -45,8 +45,7 @@ class PChannel : public TObject {
 
     ~PChannel() {
 	// Channel destructor
-	if (ipid.GetArray())
-	    ipid.~TArrayI();
+	// ipid is a value member; its destructor is invoked automatically.
 	// necessary, because of constr 2+3, for constr 1 also better
 	if (ptcls && ptcls_extern>=0) {
 	    for(int i=ptcls_extern; i<=n; i++)
diff --git a/include/PUtils.h b/include/PUtils.h
index e57139d..6cccadf 100644
--- a/include/PUtils.h
+++ b/include/PUtils.h
@@ -64,6 +64,14 @@ class PUtilsREngine : public TObject {
 
     void SetSeed(UInt_t s);
 
+    // Non-owning RNG injection for an external event generator. The caller
+    // restores the returned pointer before its engine goes out of scope.
+    TRandom *SetExternalRandom(TRandom *engine) {
+        TRandom *previous = rnd;
+        rnd = engine;
+        return previous;
+    }
+
     Double_t lambda(double M, double m1, double m2) {
 	return PKinematics::lambda(M, m1, m2);
     }
@@ -80,7 +88,7 @@ class PUtilsREngine : public TObject {
 
  private:
 
-    TRandom3 *rnd;
+    TRandom *rnd;
 
     ClassDef(PUtilsREngine, 0) //Pluto Utilities Class (random wrapper)
 };
@@ -192,4 +200,3 @@ class PUtils : public TObject {
 
 
 #endif // _PUTILS_H_
-
diff --git a/plugins/eta_decays/src/PEtaDoubleDalitz.cc b/plugins/eta_decays/src/PEtaDoubleDalitz.cc
index 0dc8bf1..063855f 100644
--- a/plugins/eta_decays/src/PEtaDoubleDalitz.cc
+++ b/plugins/eta_decays/src/PEtaDoubleDalitz.cc
@@ -18,7 +18,7 @@ PDistribution* PEtaDoubleDalitz::Clone(const char*) const {
 
 PEtaDoubleDalitz::PEtaDoubleDalitz(const Char_t *id, const Char_t *de, Int_t key) : PChannelModel(id, de, key) {
     //Constructor
-    gRand = new TRandom2();
+    gRand = NULL; // Random numbers come from the injected PUtils engine.
     dil1 = dil2 = parent = NULL;
 };
 
@@ -30,7 +30,8 @@ Double_t PEtaDoubleDalitz::Gen2lepton1(Double_t m) {
     Double_t WTD;
     Double_t AM;
     do {
-        gRand->RndmArray(2, ARAND);
+        ARAND[0] = PUtils::sampleFlat();
+        ARAND[1] = PUtils::sampleFlat();
         AM = MEl2*TMath::Exp(TMath::Log(m/MEl2)*ARAND[0]);
         Double_t AM2 = AM*AM;
         Double_t RE  = MEl2*MEl2/AM2;
@@ -76,6 +77,7 @@ Bool_t PEtaDoubleDalitz::SampleMass(void) {
     Double_t weight = 1.;
     Double_t mVV[2];
     do {
+	weight = 1.; // Each proposal has its own weight; do not accumulate rejections.
 	mVV[0] = Gen2lepton1(MEta);//IM of the 1-st pair e+e-
 	mVV[1] = Gen2lepton1(MEta);//IM of the 2-nd pair e+e-
 
diff --git a/src/PUtils.cc b/src/PUtils.cc
index a8b85fc..7729d37 100644
--- a/src/PUtils.cc
+++ b/src/PUtils.cc
@@ -404,7 +404,7 @@ PUtilsREngine::PUtilsREngine () {
 
 void PUtilsREngine::SetSeed(UInt_t s) {
     rnd->SetSeed(s);
-    UInt_t seed = (UInt_t)rnd->Rndm()*kMaxUInt; // get a different seed for gRandom
+    UInt_t seed = static_cast<UInt_t>(rnd->Rndm()*kMaxUInt); // cast after scaling
     gRandom->SetSeed(seed);//TODOv6
 
     if (s)
